# Base URL of the AgriAlert server (no trailing /api — the client prepends it).
# Dev default is http://localhost:3000 if this var is unset.
# In production set this to your deployed server origin (e.g. https://agri-alert-api.onrender.com).
VITE_API_BASE_URL=http://localhost:3000
