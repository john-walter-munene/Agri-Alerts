/**
 * Demo farmer constants — single source of truth for the zero-click demo.
 *
 * The seed script (server/prisma/seed.js) provisions this exact phone with
 * the Bomet location + tea crop + all triggers enabled, so a reviewer can
 * land on `/` and see a fully populated dashboard immediately.
 */
export const DEMO_PHONE = "+254700000001";
export const DEMO_NAME = "Demo Farmer";
export const DEMO_LOCATION = "Kapkimolwa, Bomet";
